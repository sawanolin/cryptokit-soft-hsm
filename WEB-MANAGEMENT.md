# CryptoKit SoftHSM Manager

`CryptoKit SoftHSM Manager` 是镜像内置的 Web 管理端，与 `sdfxd`、openHiTLS 同容器运行。Web 监听 `0.0.0.0:18080`，SDF TCP 监听 `0.0.0.0:18081`；浏览器请求使用同源相对地址，因此可从局域网通过 `http://服务器IP:18080` 管理。两个端口都未内置 TLS，只应开放给可信网络或放在 HTTPS 反向代理与访问控制之后。

## 首次初始化

首次初始化只执行三件事：

1. 创建第一个超级管理员（没有默认账号或密码）；
2. 写入厂商、设备名和序列号；
3. 自动生成 `/var/lib/sdfx/master/device-integrity.key` 设备完整性密钥。

初始化不再生成 SM2、RSA 或对称业务密钥。超级管理员登录后创建系统管理员、安全管理员和审计管理员；安全管理员再进入“密码服务与密钥”配置业务密钥。

创建首个超级管理员或创建其他管理员时，Web 后端只调用一次
`SDF_GenerateRandom(8)` 生成该账户的固定盐值。浏览器按
`SM3(UTF8(口令) || 盐值)` 计算 32 字节哈希，后端只把 8 字节盐值与 32 字节
哈希的 Base64 形式写入 `/var/lib/sdfx/web/state.json`。后续登录不会重新生成盐值：
页面暂存用户刚输入的口令，查询该账户已有盐值，在浏览器内完成同一计算后清空
口令输入框并提交哈希。以后修改口令也复用该账户已经保存的盐值，不再次生成。

设备完整性密钥不提供查看、导出或修改接口，只在完全恢复出厂时删除。它是软件实现中的设备根秘密，不等同于硬件密码模块不可提取的物理密钥。

## 角色与权限

| 功能模块 | 超级管理员 | 系统管理员 | 安全管理员 | 审计管理员 |
| --- | :---: | :---: | :---: | :---: |
| 系统初始化 | 创建时执行 | 否 | 否 | 否 |
| 管理员管理 | 是 | 否 | 否 | 否 |
| 关机、重启 | 是 | 是 | 否 | 否 |
| 系统命令 | 是 | 是 | 否 | 否 |
| 备份、恢复 | 是 | 是 | 否 | 否 |
| 升级、恢复出厂 | 是 | 是 | 否 | 否 |
| 在线用户 | 否 | 是 | 否 | 否 |
| 定时任务 | 否 | 是 | 否 | 否 |
| 设备监控 | 否 | 是 | 否 | 否 |
| 时间设置 | 否 | 是 | 否 | 否 |
| 网络和 DNS | 否 | 是 | 否 | 否 |
| 设备证书 | 否 | 是 | 否 | 否 |
| 授权信息 | 否 | 是 | 否 | 否 |
| 密码服务进程管理 | 否 | 是 | 否 | 否 |
| RSA/SM2/对称密钥 | 否 | 否 | 是 | 否 |
| 日志配置和审计 | 否 | 否 | 否 | 是 |

当前版本已经提供管理员管理、设备配置与监控、在线 SDF 会话、备份恢复、恢复出厂、密码服务管理、密钥管理和审计。“服务管理”中的启停/重启仅控制容器内 `sdfxd` 密码服务进程，不等同于关闭、重启 Docker 容器或宿主机。容器/主机关机重启、任意系统命令、定时任务、主机时间、网络/DNS、设备证书、授权信息和在线 Web 用户页面尚未暴露；表中这些行定义后续模块必须遵守的角色边界，不能据此理解为当前已有对应页面。

前端只显示当前角色页面，后端每个 API 还会独立校验角色和 CSRF，手工构造请求不能绕过权限。超级管理员可创建、停用、改角色、重置密码或删除其他管理员；系统至少保留一个启用的超级管理员。

## UKey 身份鉴别

超级管理员可以在“管理员管理”中点击账户的“UKey”按钮，上传：

1. UKey 中对应的用户签名证书（X.509 DER `.cer`）；
2. 签发该用户证书的 CA 证书（X.509 DER `.cer`）。

上传的 CA 证书作为该账户的信任锚，可以是根 CA，也可以是管理员已经通过
其他渠道确认可信的中间 CA。保存配置和每次 UKey 登录时都会检查：DER/X.509
结构、SM2 公钥曲线、SM2-with-SM3 签名算法、用户和 CA 有效期、Issuer/Subject、
用户证书签名、用户证书 `digitalSignature`、CA 证书 `CA=true` 和
`keyCertSign`、以及存在时的 AKI/SKI 对应关系。教学版当前不执行在线
CRL/OCSP 查询。

每个管理员账户由超级管理员选择一种登录方式：

1. **用户名+口令**：只允许口令选项卡登录；
2. **用户名+口令+UKey**：创建挑战前先校验服务器端登录口令，随后还必须
   完成 UKey 签名，两个因子均通过才创建管理会话。

选择组合方式前必须先配置并启用 UKey；停用或移除 UKey 前必须先将账户改回
“用户名+口令”，防止误操作导致账户不可登录。升级时原有免口令配置会自动
回到口令登录方式。

登录主页面只输入用户名和服务器口令。服务器口令校验通过后，浏览器打开
`http://127.0.0.1:18088/bridge/pin` 小窗口输入 UKey PIN。该窗口与 Agent
同源，PIN 不进入管理主页面，也不包含在任何发往软件服务器密码机的请求中。
挑战响应流程如下：

```text
浏览器 -> Web：用户名（查询账户固定盐值）
浏览器：SM3(UTF8(服务器口令) || 固定8字节盐值)
浏览器 -> Web：用户名 + 32字节口令哈希
Web -> sdfxd：SDF_GenerateRandom(32)
Web -> 浏览器：结构化一次性挑战（60 秒）
浏览器 -> 本机 PIN 窗口：挑战 + SM2 ID（postMessage，不含 PIN）
本机 PIN 窗口 -> Agent：挑战 + PIN + SM2 ID（同源）
Agent -> UKey：SKF_VerifyPIN / Digest / ECCSignData / ExportCertificate
本机 PIN 窗口 -> 浏览器：r||s 签名 + 签名证书（postMessage，不含 PIN）
浏览器 -> Web：r||s 签名 + 签名证书
Web -> sdfxd：SM2 预处理摘要 + SDF_ExternalVerify_ECC
Web：证书指纹、证书链、挑战会话、有效期和一次性状态全部通过后创建登录会话
```

挑战正文固定包含协议版本、用途、用户名、会话号、32 字节随机数、签发时间和
失效时间；挑战与发起浏览器的 HttpOnly Cookie 绑定，成功后立即标记已用，重复
提交会被拒绝。SM2 用户 ID 固定为 `1234567812345678`。客户端插件安装、
32/64 位厂商 SKF DLL 选择和证书导出见 [ukey-agent/README.md](ukey-agent/README.md)。

回环地址指向登录用户的客户电脑。管理主页面不直接请求回环接口，而是打开
Agent 的本地顶层 PIN 窗口，因此 HTTP 和 HTTPS 管理页面使用相同流程。登录页
提供版本匹配的 Agent 下载按钮；窗口未就绪时弹出“启动、重新检测或下载”提示。
Agent 仍只监听 `127.0.0.1`，不得改为 `0.0.0.0`。

## 服务管理

系统管理员可在“服务管理”页面查看：

- SDF TCP 监听地址和端口；
- Supervisor 进程状态、PID、启动时间、最近停止时间和运行时长；
- 本次启动累计请求数、活动 SDF 会话数和密钥数；
- 进程已启动但协议尚不可用时的诊断信息。

“启动服务”“停止服务”“重启服务”会真实控制同一容器内的 `sdfxd`
进程，Web 管理进程始终保持运行。每个操作要求输入 `START SERVICE`、
`STOP SERVICE` 或 `RESTART SERVICE`，后端同时检查系统管理员角色、CSRF，
并记录成功或失败审计。停止或重启会断开所有 SDF TCP 客户端和活动会话，
累计请求数会在进程重新启动后从零计数；`/var/lib/sdfx` 数据卷中的密钥、
账号、设备信息和配置不会被删除。

Web 通过权限为 `0700`、所有者为 `sdfx:sdfx` 的
`/run/sdfx/supervisor.sock` 调用 Supervisor XML-RPC，只允许控制固定的
`sdfxd` 进程，不接受任意进程名或系统命令，也不新增对外监听端口。

## 密钥管理

安全管理员可以：

- 分别生成 SM2 或 RSA 的签名密钥、加密密钥，签名和加密索引可不同；
- 生成 1024–2048 位 RSA 密钥；
- 在“对称密钥”区生成、启停和删除对应 SDF API KEK 的 SM4 密钥；
- 在“非对称密钥”区启停、删除、导出公钥和修改私钥访问控制码；
- 公钥复制兼容局域网 HTTP：优先使用安全剪贴板，受限时回退到兼容复制或显示完整 Base64 供手动复制；
- 修改密钥索引，移动后重新计算完整性值；
- 点击“校验完整性”分别验证非对称密钥或对称密钥记录。
- 为 SM2、RSA 和对称密钥设置 0–36500 天有效期；0 表示长期有效。列表显示创建时间、到期时间和剩余天数，30 天内到期显示预警，已到期显示告警。该教学版本到期只告警，不自动停用或阻止 SDF 调用。

私钥访问控制码允许为空。为空时，应用可传入长度为 0 的访问控制码，私钥不会明文落盘，仍使用设备密钥派生的密钥加密。设置了口令时，必须先取得私钥使用权限。

SM2/RSA 持久化记录把密钥类型、算法、索引、公钥、加密私钥和元数据一起纳入 HMAC-SM3；对称密钥记录把记录版本、算法、位数、索引和密钥材料一起纳入 HMAC-SM3。HMAC 密钥均由初始化时生成的设备完整性密钥提供。校验失败会返回密钥错误，不继续使用受损记录；旧版 16 字节对称密钥文件会在首次读取时原地迁移为受保护记录。非对称密钥改索引时会重新保护记录，口令为空的私钥还会按新索引重新派生加密密钥。

## 审计和运行日志

审计管理员可查看结构化管理审计，字段包括时间、级别、分类、管理员、来源地址、请求号、HTTP 方法、路径、操作、目标、结果、详情和 User-Agent。页面可按时间段、精确级别、日志类型、执行结果、管理员、操作、目标、来源地址、请求号、HTTP 方法、接口路径、全文关键字、排序和最大条数组合筛选；导出时还可选择 TXT、CSV、JSONL、字段、表头、顺序和最大条数。SDF 返回错误状态码以及连接密码服务失败会统一记录为 `ERROR` 级别、`sdf` 类型，便于只导出密码调用失败日志。

容器运行日志还会记录服务启动、模块、进程/线程、函数和源码位置。每个 SDF 请求记录客户端、命令号、会话号、收发字节数、返回码和耗时；不会记录密码、密钥或协议载荷。查看方式：

```bash
docker logs --tail 200 cryptokit-soft-hsm
```

## 会话与安全控制

- Web 会话使用 HttpOnly、SameSite=Strict Cookie；所有变更接口要求 CSRF；
- 登录失败按来源地址限流；
- Web 状态只存每个账户的 8 字节固定盐值和 32 字节 SM3 哈希；固定哈希仍是可重放的登录凭据，生产部署应使用 HTTPS；
- UKey 挑战有效期为 60 秒、绑定发起浏览器并只能成功使用一次；
- 删除、恢复和重置等危险操作要求当前管理员密码和确认文字；
- 私有管理命令携带容器启动时随机生成的 `/run/sdfx/admin.token`，该令牌不进入镜像或数据卷；
- 密码服务进程启停通过容器内私有 Supervisor Unix 套接字完成；
- CSP、禁止嵌入、禁止缓存和请求编号响应头默认开启；
- 管理端不提供私钥或对称密钥明文导出。

## 持久化与全新设备

所有 Web 账号、口令盐值与哈希、审计、密钥和设备状态都位于 `/var/lib/sdfx`。
其中账号和口令记录的准确文件是 `/var/lib/sdfx/web/state.json`。删除并重建容器但
重新挂载同一个命名卷，会保留上次账号和口令，这是预期行为。只需要重新创建
Web 用户名和口令、同时保留密码机密钥与其他设备数据时，可先备份再删除这个
`state.json`，随后重启容器并重新初始化；不要为此删除整个数据卷。

旧版本的 PBKDF2 账户记录无法在不知道原始口令的情况下转换为本版固定盐值
SM3 记录，因此不做自动迁移。升级已有部署后若登录页提示账户口令格式无效，
请备份并删除上述 `state.json`，再重新创建超级管理员和其他管理员。恢复包含旧
`state.json` 的历史备份也会恢复旧账户格式，需要执行同样的账户重建步骤。

## 验证

隔离空卷的综合回归：

```powershell
python tests\rbac_integrity_e2e.py --base-url http://127.0.0.1:28080
```

该测试覆盖四角色越权阻断、空口令 SM2/RSA、签名与加密不同索引、改索引、非对称/对称密钥 HMAC-SM3 校验、RSA 自检、对称密钥、备份、审计配置和 TXT 导出。Windows 侧另用 `tests/test1.c` 验证 SM2 封装回环，用 `tests/rsa_e2e.c` 验证 RSA 外部/内部运算与 IPK/ISK 会话密钥回环。

## 当前限制

- 业务 TCP 和 Web 默认没有 TLS 或双向认证；
- 备份包校验路径、类型、大小和校验和，但未做整包口令加密；
- 设备完整性密钥保存在软件数据卷中，拥有宿主机或 Docker 卷权限的攻击者仍可读取；
- 本项目用于开发、学习和接口联调，不代表通过商用密码产品认证。
## 源码与许可证

Web 侧栏在初始化、登录和管理状态下均提供“获取源代码”入口，指向
<https://github.com/sawanolin/cryptokit-soft-hsm>，并可直接查看镜像内随附
的完整 GNU AGPL v3.0 正文。CryptoKit SoftHSM 原创代码与修改采用
`AGPL-3.0-only`；SDFX、openHiTLS 原始材料继续适用各自既有许可证。
修改版本通过网络提供服务时，应保证源码入口指向与实际部署版本对应的源码。
